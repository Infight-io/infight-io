{
  "name": "@infight/server",
  "version": "0.1.0",
  "description": "Infight.io serverless backend",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "sam local start-api",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "coverage": "vitest --coverage",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "fastify": "^4.24.0",
    "@fastify/aws-lambda": "^4.0.0",
    "@aws-sdk/client-dynamodb": "^3.470.0",
    "@aws-sdk/client-ssm": "^3.470.0",
    "electrodb": "^2.14.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "@vitest/ui": "^1.0.0",
    "c8": "^9.0.0",
    "supertest": "^6.3.0",
    "@types/supertest": "^6.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
